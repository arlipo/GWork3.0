//TODO
namespace Open.Facade.Rule {
    public class RuleErrorView : StringVariableView {

        //public RuleError() { }

        //public RuleError(string name, string value) : base(name, value) { }

        //public static RuleError InContext(string name) => new RuleError(name,
        //    inContext);

        //public static RuleError InOverride(string name) => new RuleError(name,
        //    inOverride);

        //public static RuleError InRule(string name) => new RuleError(name,
        //    inRule);

        //public static RuleError NotAuthorized(string name) => new RuleError(
        //    name,notAuthorized);

        //public static RuleError NotSigned(string name) => new RuleError(name,
        //    notSigned);

        //public new static RuleError Random() {
        //    var r = new RuleError();
        //    r.setRandomValues();
        //    return r;
        //}

        //public static RuleError Unknown(string name = null) => new RuleError(
        //    name ?? string.Empty,unknown);

        //internal const string inContext = "Context error";

        //internal const string inOverride = "Override error";

        //internal const string inRule = "Rule error";

        //internal const string notSigned = "Override is not signed error";

        //internal const string notAuthorized =
        //    "Override is not authorized  error";

        //internal const string unknown = "Unknown error";

    }
}

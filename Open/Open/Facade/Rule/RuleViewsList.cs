//TODO

namespace Open.Facade.Rule {
    public class RuleViewsList //: Set<Rule>
    {

        //public static Rule Add(string name,RuleElements e) {
        //    var r = new Rule(name,e);
        //    return add(r);
        //}

        //public static Rule Find(string id) {
        //    return Instance.Find(o => o.IsThis(id)) ?? Rule.Empty;
        //}

        //public static Rules Get()
        //{
        //    var rules = new Rules();
        //    Instance.Clear();
        //    Instance.AddRange(ExchangeRateTypes.GetRules());
        //    rules.AddRange(Instance);
        //    return rules;
        //}

        //public static Rule Insert(int idx,string name,RuleElements r) {
        //    var m = new Rule(name,r);
        //    return insert(idx,m);
        //}

        //public static Rules Instance { get; } = new Rules();

        //public static Rules Random() {
        //    var l = new Rules();
        //    var c = GetRandom.Count(3);
        //    for (var i = 0; i < c; i++)
        //        l.Add(Rule.Random());
        //    return l;
        //}

        //private static Rule add(Rule r) { return insert(0,r); }

        //private static Rule insert(int idx,Rule r) {
        //    var x = Find(r.Name);
        //    if(!IsNull(x) && !x.IsEmpty())
        //        return x;
        //    x = Instance.Find(o => o.IsSameContent(r));
        //    if (!IsNull(x) && !x.IsEmpty())
        //        return x;
        //    r.UniqueId = r.Name;
        //    if(idx < 1)
        //        Instance.Add(r);
        //    else
        //        Instance.Insert(idx,r);
        //    return r;
        //}
        //public static Rule AddRule(Rule r) {
        //    return add(r);
        //}
    }
}

